import { RenameActionsMap } from './interface';

const renameMap: RenameActionsMap = {
  showModal: 'show',
  updateModal: 'update',
  clickModal: 'click',
  destroyModal: 'destroy',
  submitModal: 'submit',
  hideModal: 'hide',
};

export default renameMap;
